#include <bits/stdc++.h>
#define loop(i,0,b) for( int i=a ;i <b ; ++i)
#define elif()  else if()

using namespace std;
int M;

bool cmp(int a, int b){

}


int main(){
    int N;
    while(scanf("%d%M",&N,&M)==1,M+N){
        vector<int> num(N);
        loop(i,0,N)scanf("%d",&num[i]);

    }

    return 0;
}
