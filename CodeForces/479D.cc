#include<bits/stdc++.h>


using namespace std;


int main(){
    int n ; 
    cin >> n;
    set<int> numS;
    vector<int> numV;
    for(int i = 0 ; i < n ; ++i){
        int num; cin >> num;
        numS.insert(num);
        numV.push_back(num);
    }
    




    return 0;
}